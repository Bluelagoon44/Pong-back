// Ex: parcours complet création annonce + inscription
const request = require('supertest');
const app = require('../../app');

describe('Parcours utilisateur complet', () => {
  it('should allow user registration, login, create ad, and register', async () => {
    // ...simuler tout le flow avec supertest
    // (à compléter selon tes routes)
    expect(true).toBe(true);
  });
});
